\* TLC Configuration File
\* Generated for module: ECommercePlatform_PaymentProcessing

SPECIFICATION Spec

CONSTANTS
    pending = "pending"
    processing = "processing"
    succeeded = "succeeded"
    failed = "failed"
    retrying = "retrying"
    circuit_open = "circuit_open"
    retry_pending = "retry_pending"
    retry_succeeded = "retry_succeeded"
    retry_exhausted = "retry_exhausted"
    circuitbreaker_closed = "circuitbreaker_closed"
    circuitbreaker_open = "circuitbreaker_open"
    circuitbreaker_half_open = "circuitbreaker_half_open"
    idempotent_idle = "idempotent_idle"
    idempotent_processing = "idempotent_processing"
    idempotent_completed = "idempotent_completed"
    idempotent_duplicate = "idempotent_duplicate"

INVARIANTS
    TypeOK

PROPERTIES
    Prop_eventual_outcome
    Prop_CircuitBreaker_eventual_recovery
    Prop_Idempotent_exactly_once
    Prop_Idempotent_idempotent_response

\* Checking options
CHECK_DEADLOCK FALSE
