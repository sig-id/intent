\* TLC Configuration File
\* Generated for module: PaymentPlatform_TransactionLifecycle

SPECIFICATION Spec

CONSTANTS
    pending = "pending"
    validating = "validating"
    authorizing = "authorizing"
    captured = "captured"
    settled = "settled"
    failed = "failed"
    refunded = "refunded"
    retry_pending = "retry_pending"
    retry_succeeded = "retry_succeeded"
    retry_exhausted = "retry_exhausted"
    circuitbreaker_closed = "circuitbreaker_closed"
    circuitbreaker_open = "circuitbreaker_open"
    circuitbreaker_half_open = "circuitbreaker_half_open"
    timeout_pending = "timeout_pending"
    timeout_completed = "timeout_completed"
    timeout_timed_out = "timeout_timed_out"

INVARIANTS
    TypeOK

PROPERTIES
    Prop_eventual_completion
    Prop_no_partial_settlement
    Prop_CircuitBreaker_eventual_recovery

\* Checking options
CHECK_DEADLOCK FALSE
